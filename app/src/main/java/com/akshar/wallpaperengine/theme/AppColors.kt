package com.akshar.wallpaperengine.theme

import androidx.compose.ui.graphics.Color

/**
 * Sophisticated OLED-friendly technical palette.
 * Principle: Strict restraint from over-use of purple. Black should dominate the UI.
 */
object AppColors {
    // Core Backgrounds
    val BackgroundPrimary = Color(0xFF050505)
    val SurfaceSecondary = Color(0xFF111114)
    val SurfaceElevated = Color(0xFF17171B)
    
    // Accents
    val ElectricLime = Color(0xFFC8FF3D)
    val MutedLime = Color(0xFF9FCB2B)
    val PrimaryContainer = Color(0xFF263000)
    
    // Typography
    val TextPrimary = Color(0xFFF3F4F6)
    val TextSecondary = Color(0xFF9CA3AF) // Cool gray
    
    // Borders & Elements
    val BorderSubtle = Color(0x1AFFFFFF)
}
